filter的那些都不是重点

getCandidateConfigurations(annotationMetadata, attributes); 自动配置的核心方法

return EnableAutoConfiguration.class;  返回被这类标注的资源，这个注解被@springbootapplication继承了，所以兜兜转转是为
了加载启动类下的所有资源

META-INF/spring.factories 自动配置的核心文件

虽然遍历的所有的自动配置类，但是仅当这个类存在，才会被加载，是不是设计的很精妙

配置文件到底能写什么？

这个HttpProperties 绑定到了配置文件中前缀为spring.http的东西
